# 错题知识图谱修复与优化计划

> **项目**: 五好伴学 K12 智能学习平台  
> **模块**: 错题知识图谱功能修复  
> **版本**: v1.0（修复版）  
> **创建时间**: 2025-11-03  
> **预计工期**: 3 周 (15 个工作日)  
> **状态**: 🔴 待开始

---

## 📋 目录

- [问题诊断总结](#问题诊断总结)
- [修复目标](#修复目标)
- [第一周：紧急修复](#第一周紧急修复)
- [第二周：核心功能](#第二周核心功能)
- [第三周：进阶功能](#第三周进阶功能)
- [验收标准](#验收标准)
- [风险控制](#风险控制)

---

## 🔍 问题诊断总结

### 现状分析

| 模块             | 期望状态   | 实际状态      | 问题           |
| ---------------- | ---------- | ------------- | -------------- |
| 数据库表         | ✅ 已创建  | ✅ 已创建     | 表结构正常     |
| 知识点关联数据   | 应有数据   | ❌ **表为空** | 关联逻辑未执行 |
| AI 学情上下文    | 应注入学情 | ❌ **未实现** | 缺少上下文构建 |
| 知识图谱快照     | 定时生成   | ❌ **无数据** | 定时任务未实现 |
| 小程序知识点筛选 | 可用       | ❌ **不可用** | 未调用 API     |
| 错题分类策略     | 支持分类   | ❌ **未实现** | API 缺少参数   |

### 核心问题

1. **知识点关联逻辑断裂**：Service 层代码存在，但调用链路不通，导致 `mistake_knowledge_points` 表无数据
2. **AI 缺少学情感知**：未注入用户知识图谱上下文，无法提供个性化建议
3. **前后端数据不匹配**：小程序期望的字段与后端返回不一致
4. **基础设施缺失**：定时任务、数据回填脚本未实现

---

## 🎯 修复目标

### 核心目标

1. ✅ **知识点自动关联**：错题创建时自动提取并关联知识点
2. ✅ **AI 学情感知**：大模型分析时了解学生学情，提供个性化建议
3. ✅ **小程序知识点筛选**：用户可按知识点筛选错题
4. ✅ **知识图谱快照**：每日自动生成学情快照
5. ✅ **错题智能分类**：区分"不会做"、"答错"、"有难度"的题

### 技术指标

- API 响应时间 < 500ms (P95)
- 知识点关联成功率 > 90%
- 代码覆盖率 > 80%
- 生产环境零停机部署

---

## 📅 第一周：紧急修复

> **目标**：修复知识点关联逻辑，实现 AI 学情感知，打通小程序筛选功能

### Day 1-2: 修复知识点自动关联逻辑

#### 任务清单

- [ ] **Task 1.1: 诊断现有代码调用链路**
  - 阅读 `src/services/learning_service.py` 中的 `create_mistake_from_question` 方法
  - 确认 `ai_feedback` 参数传递是否完整
  - 检查 `KnowledgeGraphService.analyze_and_associate_knowledge_points()` 调用条件
- [ ] **Task 1.2: 修改学习问答服务**

  - 文件：`src/services/learning_service.py`
  - 修改：确保 AI 批改结果完整传递给错题创建流程
  - 代码位置：`create_mistake_from_question` 方法

  ```python
  # 修改前
  ai_feedback = {"feedback": result.get("feedback")}

  # 修改后
  ai_feedback = {
      "feedback": result.get("feedback"),
      "knowledge_points": result.get("knowledge_points", []),  # 🎯 新增
      "error_analysis": result.get("error_analysis"),
      "is_correct": result.get("is_correct", False)
  }
  ```

- [ ] **Task 1.3: 增强知识点提取逻辑**

  - 文件：`src/services/knowledge_graph_service.py`
  - 方法：`_extract_knowledge_points()`
  - 增强：支持多种 AI 返回格式

  ```python
  def _extract_knowledge_points(self, ai_feedback: Optional[Dict[str, Any]]) -> List[Dict[str, Any]]:
      """从 AI 反馈中提取知识点（增强版）"""
      if not ai_feedback:
          return []

      # 尝试多种可能的字段名
      knowledge_points = (
          ai_feedback.get("knowledge_points") or
          ai_feedback.get("涉及知识点") or
          ai_feedback.get("知识点") or
          ai_feedback.get("related_knowledge") or
          []
      )

      # 兼容字符串列表和字典列表
      # ... 详细实现
  ```

- [ ] **Task 1.4: 强化 AI Prompt 要求返回知识点**

  - 文件：`src/services/bailian_service.py` 或相关提示词文件
  - 修改：在学习问答批改的 Prompt 中明确要求返回 JSON 格式知识点

  ```python
  prompt = """
  请分析以下题目，按 JSON 格式返回：
  {
      "is_correct": false,
      "feedback": "详细反馈...",
      "knowledge_points": [
          {
              "name": "二次函数图像",
              "relevance": 0.9,
              "error_type": "concept_misunderstanding",
              "error_reason": "对对称轴理解有误"
          }
      ],
      "error_analysis": "..."
  }
  """
  ```

- [ ] **Task 1.5: 编写数据回填脚本**

  - 文件：`scripts/backfill_knowledge_associations.py`
  - 功能：读取现有错题的 `ai_feedback`，提取知识点并创建关联
  - 执行：先在开发环境测试，再在生产环境执行

  ```python
  # 脚本大纲
  async def backfill_associations():
      # 1. 查询所有有 ai_feedback 但无知识点关联的错题
      # 2. 提取知识点
      # 3. 创建关联记录
      # 4. 更新知识点掌握度
      # 5. 记录日志
  ```

- [ ] **Task 1.6: 测试验证**
  - 开发环境创建测试错题
  - 验证 `mistake_knowledge_points` 表有数据
  - 验证小程序错题详情页能显示知识点

**验收标准**：

- ✅ 新创建的错题自动关联知识点
- ✅ `mistake_knowledge_points` 表有数据
- ✅ 小程序错题详情页显示知识点分析

---

### Day 3-4: 实现 AI 学情上下文注入

#### 任务清单

- [ ] **Task 2.1: 创建学情上下文构建器**
  - 文件：`src/services/knowledge_context_builder.py`（已存在，需增强）
  - 新增方法：`build_context_for_user(user_id, subject)`
  ```python
  class KnowledgeContextBuilder:
      """知识图谱上下文构建器"""

      async def build_context_for_user(
          self, user_id: UUID, subject: str
      ) -> str:
          """构建用户学情上下文"""
          # 1. 获取最新知识图谱快照
          snapshot = await self.snapshot_repo.get_latest(user_id, subject)

          # 2. 获取近期错题统计
          stats = await self.get_recent_mistake_stats(user_id, subject, days=7)

          # 3. 构建上下文文本
          context = f"""
  【学生学情画像】
  学科：{subject}
  整体掌握度：{snapshot.average_mastery if snapshot else 0:.1%}
  薄弱知识链：{self._format_weak_chains(snapshot.weak_chains if snapshot else [])}
  优势领域：{self._format_strong_areas(snapshot.strong_areas if snapshot else [])}
  ```

【近期错题统计】
最近 7 天错题数：{stats['total_mistakes']}
常见错误类型：{stats['common_error_types']}
待复习知识点：{stats['review_pending_points']}
"""
return context

````

- [ ] **Task 2.2: 修改 BailianService 支持上下文注入**
- 文件：`src/services/bailian_service.py`
- 修改：`chat_completion()` 方法添加 `user_context` 参数

```python
async def chat_completion(
    self,
    messages: List[Dict[str, str]],
    user_context: Optional[str] = None,  # 🎯 新增
    **kwargs
) -> Dict[str, Any]:
    """调用百炼 AI（支持学情上下文）"""
    if user_context:
        # 在 system prompt 前注入上下文
        messages = [
            {"role": "system", "content": user_context},
            *messages
        ]

    # ... 原有逻辑
````

- [ ] **Task 2.3: 修改学习问答服务调用 AI 时注入上下文**

  - 文件：`src/services/learning_service.py`
  - 方法：AI 批改相关方法

  ```python
  # 在调用 AI 前构建上下文
  context_builder = KnowledgeContextBuilder(self.db)
  user_context = await context_builder.build_context_for_user(
      user_id, question.subject
  )

  # 调用 AI 时传入上下文
  response = await self.bailian_service.chat_completion(
      messages=[...],
      user_context=user_context,  # 🎯 注入学情
      stream=False
  )
  ```

- [ ] **Task 2.4: 错题创建时也注入学情上下文**

  - 文件：`src/services/mistake_service.py`
  - 场景：手动创建错题时，如果需要 AI 分析也注入上下文

- [ ] **Task 2.5: 测试 AI 效果对比**
  - 准备测试用例：同一题目，有学情 vs 无学情
  - 对比 AI 反馈质量
  - 记录效果差异

**验收标准**：

- ✅ AI 批改时能获取到用户学情上下文
- ✅ AI 反馈中体现个性化建议
- ✅ 上下文长度控制在 2000 字符以内

---

### Day 5: 小程序知识点筛选联调

#### 任务清单

- [ ] **Task 3.1: 前端加载知识点选项**

  - 文件：`miniprogram/pages/mistakes/list/index.js`
  - 新增方法：`loadKnowledgePointOptions()`

  ```javascript
  /**
   * 加载知识点筛选选项
   */
  async loadKnowledgePointOptions() {
    if (!this.data.selectedSubject) {
      return;
    }

    try {
      const response = await mistakesApi.getKnowledgePointList({
        subject: this.data.selectedSubject,
        min_count: 1
      });

      if (response && response.knowledge_points) {
        const options = ['全部', ...response.knowledge_points.map(kp => kp.name)];
        this.setData({
          knowledgePointOptions: options
        });
      }
    } catch (error) {
      console.error('加载知识点选项失败', error);
    }
  }
  ```

- [ ] **Task 3.2: 学科切换时重新加载知识点**

  - 文件：同上
  - 修改：`onSubjectChange` 方法

  ```javascript
  onSubjectChange(e) {
    const subject = e.detail;
    this.setData({
      selectedSubject: subject,
      selectedKnowledgePoint: '' // 重置知识点选择
    });
    this.loadKnowledgePointOptions(); // 🎯 新增
    this.loadMistakesList();
  }
  ```

- [ ] **Task 3.3: 知识点筛选 UI 激活**

  - 文件：`miniprogram/pages/mistakes/list/index.wxml`
  - 确认筛选弹窗中知识点选择器正常工作

- [ ] **Task 3.4: 后端 API 性能验证**

  - 验证 `/api/v1/knowledge-graph/knowledge-points` 响应速度
  - 检查数据库查询是否有索引
  - 必要时添加缓存

- [ ] **Task 3.5: 端到端测试**
  - 小程序选择学科 → 看到知识点选项
  - 选择知识点 → 错题列表正确筛选
  - 清除筛选 → 恢复全部错题

**验收标准**：

- ✅ 小程序可按知识点筛选错题
- ✅ 知识点选项动态加载
- ✅ 筛选响应时间 < 500ms

---

## 📅 第二周：核心功能

> **目标**：实现错题分类策略，启动知识图谱快照定时任务

### Day 6-7: 实现错题分类策略

#### 任务清单

- [ ] **Task 4.1: 确定分类方案**

  - 选择：扩展 `source` 字段（推荐，无需迁移）
  - 新值：
    - `learning_empty` - 不会做的题（空着提交）
    - `learning_wrong` - 答错的题
    - `learning_hard` - 有难度的题
    - `manual` - 手动添加
    - `homework` - 作业

- [ ] **Task 4.2: 修改学习问答创建错题逻辑**

  - 文件：`src/services/learning_service.py`
  - 修改：`create_mistake_from_question` 方法

  ```python
  # 根据问题类型和AI判断结果确定source
  if question.answer is None or question.answer.strip() == "":
      source = "learning_empty"  # 空题
  elif ai_result.get("is_correct") == False:
      source = "learning_wrong"  # 答错
  elif difficulty_level >= 3:
      source = "learning_hard"  # 难题
  else:
      source = "learning"  # 默认
  ```

- [ ] **Task 4.3: 后端 API 支持 category 参数**

  - 文件：`src/api/v1/endpoints/mistakes.py`
  - 修改：`get_mistake_list` 端点

  ```python
  async def get_mistake_list(
      page: int = Query(1, ge=1),
      page_size: int = Query(20, ge=1, le=100),
      subject: Optional[str] = Query(None),
      mastery_status: Optional[str] = Query(None),
      knowledge_point: Optional[str] = Query(None),
      category: Optional[str] = Query(None, description="错题类型筛选"),  # 🎯 新增
      source: Optional[str] = Query(None, description="来源筛选"),  # 🎯 新增
      search: Optional[str] = Query(None),
      user_id: UUID = Depends(get_current_user_id),
      db: AsyncSession = Depends(get_db),
  ):
      filters = {}
      if category:
          filters["category"] = category
      if source:
          filters["source"] = source
      # ...
  ```

- [ ] **Task 4.4: Repository 层实现分类查询**

  - 文件：`src/repositories/mistake_repository.py`
  - 方法：`get_list` 或新增查询方法

  ```python
  # 根据 category 映射到 source
  if filters.get("category"):
      category = filters["category"]
      if category == "empty_question":
          conditions.append(MistakeRecord.source == "learning_empty")
      elif category == "wrong_answer":
          conditions.append(MistakeRecord.source == "learning_wrong")
      elif category == "hard_question":
          conditions.append(MistakeRecord.source == "learning_hard")
  ```

- [ ] **Task 4.5: 小程序端激活分类筛选**

  - 文件：`miniprogram/pages/mistakes/list/index.js`
  - 确认 `selectedCategory` 参数传递给 API

- [ ] **Task 4.6: 测试分类功能**
  - 创建不同类型错题
  - 验证筛选正确性
  - 检查数据统计

**验收标准**：

- ✅ 可区分"不会做"、"答错"、"难题"
- ✅ 小程序分类筛选可用
- ✅ 统计数据准确

---

### Day 8-9: 知识图谱快照定时任务

#### 任务清单

- [ ] **Task 5.1: 创建定时任务文件**

  - 文件：`src/tasks/knowledge_graph_tasks.py`（新建）

  ```python
  """知识图谱定时任务"""
  from celery import shared_task
  from sqlalchemy import select
  from src.core.database import get_async_session
  from src.services.knowledge_graph_service import KnowledgeGraphService

  @shared_task
  async def generate_daily_snapshots():
      """每日凌晨3点生成知识图谱快照"""
      async with get_async_session() as db:
          service = KnowledgeGraphService(db)

          # 1. 获取有错题记录的用户
          users = await get_users_with_mistakes(db)

          # 2. 为每个用户生成快照
          for user_id, subjects in users:
              for subject in subjects:
                  try:
                      await service.create_snapshot(user_id, subject)
                      logger.info(f"生成快照: user={user_id}, subject={subject}")
                  except Exception as e:
                      logger.error(f"生成快照失败: {e}")
  ```

- [ ] **Task 5.2: 配置 Celery 调度**

  - 文件：`src/core/celery_app.py` 或配置文件

  ```python
  from celery.schedules import crontab

  CELERY_BEAT_SCHEDULE = {
      'generate-daily-snapshots': {
          'task': 'src.tasks.knowledge_graph_tasks.generate_daily_snapshots',
          'schedule': crontab(hour=3, minute=0),  # 每天凌晨3点
      },
  }
  ```

- [ ] **Task 5.3: 创建手动触发接口**

  - 文件：`src/api/v1/endpoints/knowledge_graph.py`
  - 用途：测试和手动触发

  ```python
  @router.post("/snapshots/generate", response_model=SuccessResponse)
  async def trigger_snapshot_generation(
      subject: str = Query(..., description="学科"),
      user_id: UUID = Depends(get_current_user_id),
      db: AsyncSession = Depends(get_db),
  ):
      """手动触发快照生成（测试用）"""
      service = KnowledgeGraphService(db)
      snapshot = await service.create_snapshot(user_id, subject)
      return SuccessResponse(
          success=True,
          message=f"快照生成成功",
          data={"snapshot_id": str(snapshot.id)}
      )
  ```

- [ ] **Task 5.4: 完善 create_snapshot 方法**

  - 文件：`src/services/knowledge_graph_service.py`
  - 确保：
    - 构建知识图谱
    - 分析薄弱链
    - 生成学情画像
    - 保存到数据库

- [ ] **Task 5.5: 测试定时任务**
  - 手动触发快照生成
  - 验证数据库有快照数据
  - 检查快照内容完整性
  - 测试定时调度（可改为 1 分钟触发测试）

**验收标准**：

- ✅ 快照表每日自动更新
- ✅ 手动触发接口可用
- ✅ 快照数据完整（知识点、薄弱链、学情画像）

---

### Day 10: 集成测试与性能优化

#### 任务清单

- [ ] **Task 6.1: 端到端测试**

  - 创建错题 → 自动关联知识点 → 快照生成 → AI 使用学情
  - 小程序完整流程测试
  - 边界情况测试（无错题、无知识点等）

- [ ] **Task 6.2: 性能测试**

  - 使用 JMeter/Locust 压力测试
  - 关键 API 响应时间测试
  - 数据库查询性能分析

- [ ] **Task 6.3: SQL 查询优化**

  - 检查慢查询日志
  - 添加必要索引
  - 优化 N+1 查询

- [ ] **Task 6.4: 添加缓存**

  - 知识图谱快照缓存（Redis）
  - 知识点列表缓存
  - 学情上下文缓存

- [ ] **Task 6.5: 错误处理加固**
  - 添加异常捕获
  - 降级处理（AI 失败、数据库慢等）
  - 日志完善

**验收标准**：

- ✅ API 响应时间 < 500ms (P95)
- ✅ 无严重性能瓶颈
- ✅ 错误处理完善

---

## 📅 第三周：进阶功能

> **目标**：实现薄弱知识链分析和智能复习推荐

### Day 11-13: 薄弱知识链分析

#### 任务清单

- [ ] **Task 7.1: 补全知识点关系数据**

  - 文件：数据导入脚本
  - 导入：教材大纲中的知识点前置关系
  - 表：`knowledge_nodes` 的 `prerequisite_relations` 字段

- [ ] **Task 7.2: 实现薄弱链分析算法**

  - 文件：`src/services/knowledge_graph_service.py`
  - 方法：`analyze_weak_chains()`
  - 算法：
    1. 从 knowledge_nodes 获取前置关系
    2. DFS 遍历知识点网络
    3. 找出平均掌握度 < 0.5 的链路
    4. 识别瓶颈节点

- [ ] **Task 7.3: 前端可视化展示**

  - 页面：学习报告页
  - 展示：薄弱知识链列表
  - 交互：点击跳转到相关错题

- [ ] **Task 7.4: 测试验证**
  - 构造测试数据
  - 验证算法正确性
  - 检查前端展示

**验收标准**：

- ✅ 能识别薄弱知识链
- ✅ 瓶颈节点标识准确
- ✅ 前端可视化清晰

---

### Day 14-15: 智能复习推荐

#### 任务清单

- [ ] **Task 8.1: 实现优先级算法**

  - 文件：`src/services/knowledge_graph_service.py`
  - 方法：`recommend_review_path()`
  - 公式：

  ```python
  priority = (
      (1 - mastery_level) * 0.4 +        # 掌握度低优先
      prerequisite_weight * 0.3 +        # 前置知识点优先
      forgetting_risk * 0.2 +            # 遗忘风险高优先
      related_chain_weak * 0.1           # 关联链薄弱优先
  )
  ```

- [ ] **Task 8.2: 今日复习页整合**

  - 文件：`miniprogram/pages/mistakes/today-review/index.js`
  - 调用推荐 API
  - 按优先级排序展示

- [ ] **Task 8.3: 测试推荐效果**
  - 用户反馈收集
  - 推荐准确性评估
  - 算法调优

**验收标准**：

- ✅ 推荐逻辑合理
- ✅ 今日复习页可用
- ✅ 用户满意度良好

---

## ✅ 验收标准

### 功能验收

| 功能             | 验收标准                            | 状态 |
| ---------------- | ----------------------------------- | ---- |
| 知识点自动关联   | 新错题自动关联知识点，关联表有数据  | ⬜   |
| AI 学情感知      | AI 批改时能看到用户学情，反馈个性化 | ⬜   |
| 小程序知识点筛选 | 可按知识点筛选错题，选项动态加载    | ⬜   |
| 知识图谱快照     | 快照表每日更新，数据完整            | ⬜   |
| 错题分类         | 可区分"空题"、"答错"、"难题"        | ⬜   |
| 薄弱链分析       | 能识别薄弱链和瓶颈节点              | ⬜   |
| 智能推荐         | 复习路径推荐合理                    | ⬜   |

### 性能验收

| 指标               | 目标        | 状态 |
| ------------------ | ----------- | ---- |
| API 响应时间 (P95) | < 500ms     | ⬜   |
| 知识点关联成功率   | > 90%       | ⬜   |
| 数据库查询优化     | 无 N+1 查询 | ⬜   |
| 代码覆盖率         | > 80%       | ⬜   |

### 安全验收

| 项目     | 检查点                 | 状态 |
| -------- | ---------------------- | ---- |
| 数据权限 | 用户只能访问自己的数据 | ⬜   |
| SQL 注入 | 参数化查询，无拼接 SQL | ⬜   |
| 错误处理 | 异常不暴露敏感信息     | ⬜   |
| 日志脱敏 | 敏感数据已脱敏         | ⬜   |

---

## ⚠️ 风险控制

### 生产部署风险

| 风险             | 影响 | 应对措施                                                                  |
| ---------------- | ---- | ------------------------------------------------------------------------- |
| 数据回填脚本失败 | 高   | 1. 先在开发环境测试<br>2. 生产执行前备份数据库<br>3. 使用事务，失败可回滚 |
| 性能下降         | 中   | 1. 压力测试验证<br>2. 添加索引和缓存<br>3. 监控响应时间                   |
| AI 调用成本增加  | 中   | 1. 控制上下文长度<br>2. 缓存学情快照<br>3. 设置调用频率限制               |

### 开发风险

| 风险               | 概率 | 应对措施                                                |
| ------------------ | ---- | ------------------------------------------------------- |
| 知识点提取准确率低 | 中   | 1. 优化 Prompt<br>2. Few-shot 学习<br>3. 人工审核机制   |
| 前后端数据不匹配   | 低   | 1. 严格按 Schema 开发<br>2. 集成测试覆盖<br>3. 版本控制 |
| 定时任务失败       | 低   | 1. 异常捕获和重试<br>2. 监控告警<br>3. 手动触发接口兜底 |

### 回滚方案

1. **数据库回滚**：保留旧数据，新表删除不影响现有功能
2. **代码回滚**：Git 回退到稳定版本
3. **服务降级**：关闭知识图谱功能，保留基础错题功能

---

## 📝 开发规范

### 代码规范

- Python: PEP 8 + Black 格式化
- JavaScript: ESLint + Prettier
- 提交信息: `类型(范围): 描述`

### 测试规范

- 单元测试覆盖率 > 80%
- 关键路径必须有集成测试
- 性能测试验证响应时间

### 文档规范

- 代码注释清晰
- API 文档更新
- 变更记录维护

---

## 📊 进度跟踪

### 第一周进度

- Day 1-2: ⬜⬜⬜⬜⬜⬜ (0/6 任务)
- Day 3-4: ⬜⬜⬜⬜⬜ (0/5 任务)
- Day 5: ⬜⬜⬜⬜⬜ (0/5 任务)

### 第二周进度

- Day 6-7: ⬜⬜⬜⬜⬜⬜ (0/6 任务)
- Day 8-9: ⬜⬜⬜⬜⬜ (0/5 任务)
- Day 10: ⬜⬜⬜⬜⬜ (0/5 任务)

### 第三周进度

- Day 11-13: ⬜⬜⬜⬜ (0/4 任务)
- Day 14-15: ⬜⬜⬜ (0/3 任务)

---

## 🎯 快速开始

### 第一步：环境准备

```bash
# 激活虚拟环境
source venv/bin/activate

# 更新依赖
pip install -r requirements.txt

# 检查数据库状态
python scripts/diagnose.py
```

### 第二步：开始第一个任务

1. 阅读 Day 1-2 任务清单
2. 创建功能分支：`git checkout -b fix/knowledge-graph-association`
3. 开始 Task 1.1

### 第三步：提交与测试

```bash
# 提交代码
git add .
git commit -m "feat(knowledge-graph): 修复知识点自动关联逻辑"

# 运行测试
make test

# 本地验证
python -m pytest tests/services/test_knowledge_graph_service.py -v
```

---

**文档维护**：开发过程中及时更新进度，标记完成的任务  
**问题追踪**：遇到问题记录到 Issues，及时沟通解决  
**每日 Review**：每天结束前检查进度，调整计划

---

_创建时间: 2025-11-03_  
_最后更新: 2025-11-03_  
_文档版本: v1.0_
